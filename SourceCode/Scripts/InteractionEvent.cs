using UnityEngine.Events;
using UnityEngine;

// Se hace referencia al evento de Unity OnInteract (al interactuar)
public class InteractionEvent : MonoBehaviour
{
    public UnityEvent OnInteract;
}
